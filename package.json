{
  "name": "phi",
  "version": "1.0.047",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "version:status": "node scripts/release-tools.js status",
    "devlog:sync": "node scripts/release-tools.js sync-devlog",
    "release:prepare": "node scripts/release-tools.js prepare-release"
  }
}
