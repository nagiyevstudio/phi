{
  "name": "phi",
  "version": "1.0.042",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "deploy:frontend": "cd frontend && npm run build",
    "deploy:backend": "cd backend && node deploy.js",
    "deploy:all": "npm run deploy:frontend && npm run deploy:backend",
    "version:status": "node scripts/release-tools.js status",
    "devlog:sync": "node scripts/release-tools.js sync-devlog",
    "release:prepare": "node scripts/release-tools.js prepare-release"
  },
  "devDependencies": {
    "ftp-deploy": "^2.4.7"
  }
}
